# 后端访问指南

## 🚀 快速启动

### 方式 1：使用启动脚本（推荐）

```bash
./start-server.sh
```

### 方式 2：手动启动

```bash
cd server
pnpm dev
```

### 方式 3：后台运行

```bash
cd server
nohup pnpm dev > /tmp/server.log 2>&1 &

# 查看日志
tail -f /tmp/server.log
```

---

## 🌐 服务地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 主服务 | http://localhost:3000 | 后端主服务地址 |
| 健康检查 | http://localhost:3000/api/health | 服务健康状态 |
| 配置接口 | http://localhost:3000/api/config/current | 获取当前配置 |

---

## 📋 配置管理后台访问

### 方式 1：通过小程序访问（推荐）

1. **启动后端服务**：
   ```bash
   ./start-server.sh
   ```

2. **打开小程序**：
   - 进入"我的"页面
   - 向下滚动找到"管理"区块
   - 点击"配置管理"

### 方式 2：通过 API 直接访问

```bash
# 获取当前配置
curl http://localhost:3000/api/config/current

# 更新配置
curl -X PUT http://localhost:3000/api/config/records/feature-taskReview \
  -H "Content-Type: application/json" \
  -d '{"value": true, "operator": "admin"}'
```

### 方式 3：通过浏览器访问

直接在浏览器中打开：
- http://localhost:3000/api/config/current

---

## 🔧 配置项说明

### 系统配置（system）

```json
{
  "version": "1.0.0",           // 配置版本号
  "environment": "dev",          // 环境：dev | prod
  "apiTimeout": 30000,          // API 超时时间（毫秒）
  "cacheTimeout": 300           // 缓存超时时间（秒）
}
```

### 功能开关（features）

```json
{
  "taskReview": false,          // 任务审核功能
  "orgSync": true,             // 组织架构同步
  "wecomNotify": true,         // 企微消息推送
  "voiceInput": true,          // 语音输入
  "taskTransfer": true,        // 任务转交
  "aiAnalysis": true,          // AI 分析
  "statistics": true           // 统计功能
}
```

### 企微配置（wecom）

```json
{
  "corpId": "",                // 企业 ID
  "agentId": 0,                // 应用 ID
  "syncInterval": 24,          // 同步间隔（小时）
  "maxDepartments": 1000,      // 最大部门数
  "notifyTemplateId": "",      // 通知模板 ID
  "autoSync": false            // 自动同步
}
```

### 业务规则（business）

```json
{
  "maxTasksPerDay": 100,       // 每日最大任务数
  "maxSubtasksPerTask": 50,    // 每个任务最大子任务数
  "taskAutoArchiveDays": 90,   // 任务自动归档天数
  "maxTeamMembers": 500,       // 最大团队成员数
  "taskAutoCloseHours": 168    // 任务自动关闭小时数
}
```

---

## 📊 监控和日志

### 查看服务状态

```bash
# 检查服务是否运行
curl http://localhost:3000/api/health

# 预期输出
{"status":"ok","timestamp":"2026-04-28T09:37:00.000Z"}
```

### 查看服务日志

```bash
# 如果使用后台运行
tail -f /tmp/server.log

# 查看最近 50 行
tail -50 /tmp/server.log

# 搜索错误
grep -i error /tmp/server.log
```

### 查看配置版本

```bash
curl http://localhost:3000/api/config/version

# 预期输出
{"version":"1.0.0","updatedAt":"2026-04-28T09:25:29.180Z"}
```

---

## 🛠️ 常见操作

### 1. 开启/关闭功能开关

```bash
# 开启任务审核
curl -X PUT http://localhost:3000/api/config/records/feature-taskReview \
  -H "Content-Type: application/json" \
  -d '{"value": true, "operator": "admin"}'

# 关闭任务审核
curl -X PUT http://localhost:3000/api/config/records/feature-taskReview \
  -H "Content-Type: application/json" \
  -d '{"value": false, "operator": "admin"}'
```

### 2. 查询功能开关状态

```bash
# 查询所有配置
curl http://localhost:3000/api/config/records?category=feature

# 预期输出
{
  "data": [
    {"key": "feature.taskReview", "value": false},
    {"key": "feature.orgSync", "value": true},
    ...
  ]
}
```

### 3. 批量更新配置

```bash
curl -X POST http://localhost:3000/api/config/batch-update \
  -H "Content-Type: application/json" \
  -d '{
    "updates": [
      {"key": "feature.taskReview", "value": true},
      {"key": "feature.orgSync", "value": false}
    ],
    "operator": "admin"
  }'
```

### 4. 查看配置历史

```bash
curl http://localhost:3000/api/config/versions

# 预期输出
{
  "data": [
    {
      "id": "1",
      "version": "1.0.0",
      "createdAt": "2026-04-28T09:25:29.180Z",
      "createdBy": "admin",
      "comment": "Initial config"
    }
  ]
}
```

### 5. 回滚配置

```bash
curl -X POST http://localhost:3000/api/config/versions/1/rollback \
  -H "Content-Type: application/json" \
  -d '{"operator": "admin"}'
```

---

## ❓ 常见问题

### Q1: 后端服务无法启动

**原因**：端口被占用

**解决方法**：
```bash
# 查找占用端口的进程
lsof -i:3000

# 杀死进程
kill -9 <PID>

# 重新启动
./start-server.sh
```

### Q2: 小程序无法连接后端

**原因**：后端服务未启动或网络问题

**解决方法**：
1. 检查后端服务是否启动：`curl http://localhost:3000/api/health`
2. 检查小程序网络请求配置
3. 查看服务日志：`tail -f /tmp/server.log`

### Q3: 配置接口返回错误

**原因**：配置格式错误

**解决方法**：
1. 检查请求参数格式
2. 查看服务日志获取详细错误信息
3. 参考配置项说明文档

### Q4: 如何重启后端服务

**解决方法**：
```bash
# 如果使用后台运行
# 查找进程
ps aux | grep "node.*main.js"

# 杀死进程
kill -9 <PID>

# 重新启动
./start-server.sh
```

---

## 📞 技术支持

如有问题，请查看：
- 后端文档：`server/README.md`
- 服务日志：`/tmp/server.log`
- 健康检查：http://localhost:3000/api/health

---

## ✅ 检查清单

启动前请确认：
- [ ] Node.js 已安装（v16+）
- [ ] 依赖已安装（`pnpm install`）
- [ ] 后端代码已编译（`pnpm build`）
- [ ] 端口 3000 未被占用
- [ ] 环境变量配置正确（可选）

---

启动后请检查：
- [ ] 服务正常启动（无报错）
- [ ] 健康检查通过（`GET /api/health`）
- [ ] 配置接口正常（`GET /api/config/current`）
- [ ] 小程序可以连接后端

---

🎉 现在你可以访问配置管理后台了！
