# 数据库索引优化指南

## 📊 索引建议

微信云开发数据库会自动分析查询性能，并给出索引建议。以下是当前项目的索引建议。

---

## 🔍 近期索引建议

### users 集合

**查询语句**：
```javascript
db.collection('users')
  .where({})
  .orderBy('created_at', 'asc')
  .get()
```

**问题**：
- 全量查询，可能扫全表
- 缺乏 `created_at` 字段的索引

**建议索引**：
```javascript
created_at: 升序
```

**快速创建链接**：
- [创建索引链接](cloud://createindex?env=cloud1-3g7j95ax4a0f4a3f&collection=users&from=console&s=%5B%7B%22field%22%3A%22created_at%22%2C%22type%22%3A1%7D%5D)

---

## 📋 建议创建的索引列表

### users 集合

| 索引字段 | 类型 | 原因 | 优先级 |
|---------|------|------|--------|
| `created_at` | 升序 | 全量查询、排序 | 高 |

### task_groups 集合

| 索引字段 | 类型 | 原因 | 优先级 |
|---------|------|------|--------|
| `user_id` | 升序 | 分组查询 | 高 |
| `order` | 升序 | 分组排序 | 中 |
| `user_id` + `order` | 复合索引 | 分组查询 + 排序 | 高 |

### tasks 集合

| 索引字段 | 类型 | 原因 | 优先级 |
|---------|------|------|--------|
| `user_id` | 升序 | 用户任务查询 | 高 |
| `status` | 升序 | 状态筛选 | 中 |
| `created_at` | 降序 | 时间排序 | 中 |
| `group_id` | 升序 | 分组筛选 | 中 |
| `user_id` + `created_at` | 复合索引 | 用户任务 + 时间排序 | 高 |

---

## 🚀 创建索引的方法

### 方式 1：微信开发者工具（推荐）

1. 打开微信开发者工具
2. 点击顶部 **「云开发」** 按钮
3. 进入 **「数据库」** 标签
4. 选择要创建索引的集合（如 `users`）
5. 点击 **「索引管理」** 按钮
6. 点击 **「添加索引」** 按钮
7. 填写索引信息：
   - **索引名称**：自定义（如 `created_at_idx`）
   - **索引字段**：选择字段和排序方式
   - **是否唯一**：选择「否」
8. 点击 **「确定」** 创建索引

### 方式 2：使用提供的快速链接

点击上面的「快速创建链接」，会自动跳转到微信开发者工具的索引创建页面。

### 方式 3：使用云函数批量创建

```javascript
// 云函数：create-indexes
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

exports.main = async (event, context) => {
  try {
    // 创建 users 集合的索引
    await cloud.database().collection('users').addIndexes([
      {
        key: { created_at: 1 },
        name: 'created_at_idx',
        unique: false
      }
    ])

    // 创建 task_groups 集合的索引
    await cloud.database().collection('task_groups').addIndexes([
      {
        key: { user_id: 1 },
        name: 'user_id_idx',
        unique: false
      },
      {
        key: { order: 1 },
        name: 'order_idx',
        unique: false
      }
    ])

    return {
      success: true,
      message: '索引创建成功'
    }
  } catch (err) {
    return {
      success: false,
      message: '索引创建失败',
      error: err
    }
  }
}
```

---

## ⚠️ 注意事项

### 1. 索引创建时间
- 创建索引可能需要几分钟
- 数据量越大，创建时间越长
- 创建期间集合可能暂时不可用

### 2. 索引数量限制
- 每个集合最多创建 20 个索引
- 每个索引最多包含 10 个字段
- 复合索引字段顺序很重要

### 3. 索引性能
- 索引会提升查询性能
- 但会降低写入性能
- 不要创建不必要的索引

### 4. 唯一索引
- 唯一索引确保字段值唯一
- 创建前需确保数据中没有重复值
- 否则创建会失败

---

## 📈 索引优化建议

### 高优先级索引

1. **users.created_at**
   - 用于全量查询、排序
   - 建议立即创建

2. **task_groups.user_id + order**
   - 复合索引，用于分组查询和排序
   - 建议立即创建

3. **tasks.user_id + created_at**
   - 复合索引，用于用户任务查询和时间排序
   - 建议立即创建

### 中优先级索引

1. **tasks.status**
   - 用于状态筛选
   - 可在数据量大时创建

2. **tasks.group_id**
   - 用于分组筛选
   - 可在数据量大时创建

---

## 🔧 索引维护

### 查看索引

1. 打开微信开发者工具
2. 进入 **「云开发」** → **「数据库」**
3. 选择集合
4. 点击 **「索引管理」** 查看所有索引

### 删除索引

1. 进入 **「索引管理」**
2. 找到要删除的索引
3. 点击 **「删除」** 按钮

### 重建索引

如果索引性能下降，可以考虑重建：
1. 删除旧索引
2. 重新创建索引

---

## 📚 参考文档

- [微信云开发数据库索引](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/database/index.html)
- [MongoDB 索引最佳实践](https://docs.mongodb.com/manual/indexes/)

---

## ✅ 检查清单

- [ ] 创建 users.created_at 索引
- [ ] 创建 task_groups.user_id 索引
- [ ] 创建 task_groups.order 索引
- [ ] 创建 tasks.user_id + created_at 复合索引
- [ ] 创建 tasks.status 索引（可选）
- [ ] 创建 tasks.group_id 索引（可选）

---

## 🎯 总结

创建索引可以显著提升查询性能，特别是在数据量大的情况下。建议按照优先级逐步创建索引。

**注意**：索引创建是可选的优化操作，不影响功能使用。如果数据量不大，可以暂时不创建索引。
